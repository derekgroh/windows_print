---
driver:
  name: exec

transport:
  name: exec

provisioner:
  name: chef_zero
  deprecations_as_errors: true
  # Allow reboots
  max_retries: 60
  wait_for_retry: 300

platforms:
  - name: windows-2016
  - name: windows-2019
